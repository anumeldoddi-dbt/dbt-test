name: 'dbt_learning'
version: '1.0.0'

config-version: 2

models: 
    dbt_learning:
       staging:
          jaffle_shop:
              +required_tests:{"unique.*|not_null": 1}
              +materialized: view
          stripe:
              +materialized: view
          marts:
              +materialized: table

    